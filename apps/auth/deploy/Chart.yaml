apiVersion: v2
version: 0.1.1

name: Luni Auth
description: A Helm chart for Luni's Auth Microservice

type: application
appVersion: '1.0.1'

dependencies:
  - name: mongodb
    version: 13.6.2
    repository: 'https://charts.bitnami.com/bitnami'
    condition: mongodb.enabled
  # - name: rabbitmq
  #   version: 11.3.2
  #   condition: rabbitmq.enabled
